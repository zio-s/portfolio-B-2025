# Supabase Configuration
# Supabase 대시보드에서 확인: Settings → API

# Supabase Project URL (예: https://xxxxx.supabase.co)
VITE_SUPABASE_URL=your_supabase_project_url

# Supabase Anon/Public Key (긴 JWT 토큰)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ================================================================
# AWS S3 + CloudFront Configuration (미디어 파일 스토리지)
# ================================================================

# AWS 리전 (서울: ap-northeast-2)
VITE_AWS_REGION=ap-northeast-2

# S3 버킷 이름
VITE_AWS_BUCKET=portfolio-semin-media

# CloudFront 배포 도메인 (예: https://d1234abcd.cloudfront.net)
VITE_CLOUDFRONT_URL=https://your-cloudfront-domain.cloudfront.net

# AWS 자격증명 (IAM 사용자 생성 후 입력)
# 주의: 프론트엔드에서 직접 사용하지 말고, 서버 사이드에서만 사용
VITE_AWS_ACCESS_KEY_ID=your_aws_access_key_id
VITE_AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# ================================================================
# 사용 방법:
# 1. 이 파일을 .env.local로 복사: cp .env.example .env.local
# 2. Supabase 대시보드에서 실제 값을 복사하여 위 변수에 입력
# 3. AWS 콘솔에서 S3 버킷과 CloudFront 배포 생성 후 정보 입력
# 4. .env.local은 절대 Git에 커밋하지 마세요! (.gitignore에 포함됨)
# ================================================================
