# Phase 6: Admin Dashboard - 완벽한 설계 및 구현 계획

**Date**: 2025-11-03
**Status**: 🎯 PRODUCTION-READY DESIGN
**목표**: 실제 프로덕션에서 사용 가능한 완벽한 Admin Dashboard

---

## 📋 Executive Summary

실제 프로젝트를 관리할 수 있는 **완벽한 Admin Dashboard**를 구현합니다. 프로덕션 레벨의 품질을 목표로 합니다.

### 핵심 기능
1. ✅ **Dashboard 메인**: 실시간 통계 (프로젝트, 댓글, 조회수, 좋아요)
2. ✅ **Project Management**: 완벽한 CRUD + Featured 설정
3. ✅ **Comment Management**: 전체 댓글 관리 + 삭제
4. ✅ **권한 체크**: Admin only, 보안 강화
5. ✅ **UX/UI**: 직관적이고 사용하기 쉬운 인터페이스

---

## 🏗️ 아키텍처 설계

### 파일 구조

```
src/
├── pages/
│   ├── DashboardPage.tsx           (✅ 기존 - 개선 필요)
│   ├── admin/
│   │   ├── AdminProjectsPage.tsx   (🆕 프로젝트 관리)
│   │   └── AdminCommentsPage.tsx   (🆕 댓글 관리)
│   └── ...
│
├── components/
│   ├── admin/
│   │   ├── StatsCard.tsx           (🆕 통계 카드 컴포넌트)
│   │   ├── ProjectTable.tsx        (🆕 프로젝트 테이블)
│   │   ├── ProjectForm.tsx         (🆕 프로젝트 생성/수정 폼)
│   │   └── CommentTable.tsx        (🆕 댓글 관리 테이블)
│   └── ...
│
├── hooks/
│   └── useAdminCheck.ts            (🆕 관리자 권한 체크)
│
├── router/
│   └── AdminRoute.tsx              (🆕 Admin 전용 라우트)
│
└── mocks/
    └── handlers/
        └── admin.ts                 (🆕 Admin API handlers)
```

---

## 🎯 Phase 6.1: Dashboard 메인 페이지 개선

### 현재 상태
- ✅ 기본 카드 레이아웃 (게시글, 프로필, 사용자)
- ❌ 통계 데이터 없음
- ❌ 프로젝트 관리 링크 없음

### 개선 사항

**1. 통계 카드 추가**
```typescript
interface DashboardStats {
  totalProjects: number;
  featuredProjects: number;
  totalComments: number;
  totalViews: number;
  totalLikes: number;
  recentActivity: Activity[];
}
```

**2. StatsCard 컴포넌트**
```typescript
interface StatsCardProps {
  title: string;
  value: number;
  icon: string;      // 이모지 or SVG
  color: string;     // Gradient color
  trend?: {
    value: number;   // +10% or -5%
    isPositive: boolean;
  };
  link?: string;     // 클릭 시 이동 경로
}
```

**3. 레이아웃**
```
┌─────────────────────────────────────────────┐
│  Dashboard                       [Dark Mode] │
├─────────────────────────────────────────────┤
│  환영합니다, Semin님!                          │
│  오늘 하루도 화이팅! 🚀                         │
├─────────────────────────────────────────────┤
│  📊 통계                                       │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐    │
│  │ 프로젝트│  │ 댓글   │  │ 조회수 │  │ 좋아요 │    │
│  │  5개  │  │  13개 │  │  142  │  │  67   │    │
│  │ +2개 │  │ +5개 │  │ +18  │  │ +10  │    │
│  └──────┘  └──────┘  └──────┘  └──────┘    │
├─────────────────────────────────────────────┤
│  🔧 빠른 액션                                  │
│  [+ 새 프로젝트] [프로젝트 관리] [댓글 관리]    │
├─────────────────────────────────────────────┤
│  📅 최근 활동                                  │
│  • 2시간 전: "Portfolio CMS"에 댓글 3개       │
│  • 5시간 전: "음저협 프로젝트" 조회수 +12     │
│  • 1일 전: "E-commerce" Featured 설정         │
└─────────────────────────────────────────────┘
```

---

## 🎯 Phase 6.2: Admin Projects Management

### 기능 명세

**1. 프로젝트 목록 (테이블)**
```
┌─────────────────────────────────────────────────────────┐
│  Admin Projects                   [+ 새 프로젝트 생성]    │
├─────────────────────────────────────────────────────────┤
│  [검색: ______] [Category: All ▼] [Featured: All ▼]     │
├─────────────────────────────────────────────────────────┤
│  ✓  Title              Category  Featured  Views Actions│
├─────────────────────────────────────────────────────────┤
│  ⭐  Portfolio CMS      Web        Yes      67   [✏️][🗑️]│
│      음저협 프로젝트     Web        Yes      45   [✏️][🗑️]│
│      E-commerce        Fullstack  No       30   [✏️][🗑️]│
│      Mobile App        Mobile     No       18   [✏️][🗑️]│
│      GraphQL API       Backend    No       12   [✏️][🗑️]│
└─────────────────────────────────────────────────────────┘
```

**2. 프로젝트 생성 모달**
```
┌─────────────────────────────────────────┐
│  새 프로젝트 생성                   [X]   │
├─────────────────────────────────────────┤
│  제목 *                                  │
│  ┌───────────────────────────────────┐  │
│  │ My Awesome Project               │  │
│  └───────────────────────────────────┘  │
│                                          │
│  설명 *                                  │
│  ┌───────────────────────────────────┐  │
│  │ 프로젝트 설명...                  │  │
│  └───────────────────────────────────┘  │
│                                          │
│  카테고리 *                              │
│  [Web ▼]                                 │
│                                          │
│  기술 스택 * (쉼표로 구분)               │
│  ┌───────────────────────────────────┐  │
│  │ React, TypeScript, Vite          │  │
│  └───────────────────────────────────┘  │
│                                          │
│  썸네일 URL                              │
│  ┌───────────────────────────────────┐  │
│  │ https://...                      │  │
│  └───────────────────────────────────┘  │
│                                          │
│  GitHub URL                             │
│  ┌───────────────────────────────────┐  │
│  │ https://github.com/...           │  │
│  └───────────────────────────────────┘  │
│                                          │
│  Live URL                               │
│  ┌───────────────────────────────────┐  │
│  │ https://...                      │  │
│  └───────────────────────────────────┘  │
│                                          │
│  콘텐츠 * (마크다운 지원)                │
│  ┌───────────────────────────────────┐  │
│  │ ## 프로젝트 개요                   │  │
│  │ 이 프로젝트는...                   │  │
│  │ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓        │  │
│  └───────────────────────────────────┘  │
│                                          │
│  ☑ Featured 설정                        │
│                                          │
│  ┌──────────┐  ┌──────────┐            │
│  │   취소   │  │ 생성하기  │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
```

**3. 프로젝트 수정**
- 생성 폼과 동일한 UI
- 기존 데이터 pre-fill
- "수정하기" 버튼

**4. 프로젝트 삭제**
- 확인 다이얼로그: "정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다."
- 관련 댓글도 함께 삭제 (Cascade delete)

### API Endpoints (MSW)

```typescript
// GET /api/admin/projects - 관리자용 프로젝트 목록 (전체 정보)
// POST /api/admin/projects - 프로젝트 생성
// PUT /api/admin/projects/:id - 프로젝트 수정
// DELETE /api/admin/projects/:id - 프로젝트 삭제
// PATCH /api/admin/projects/:id/featured - Featured 설정
```

---

## 🎯 Phase 6.3: Admin Comments Management

### 기능 명세

**1. 댓글 목록 (테이블)**
```
┌─────────────────────────────────────────────────────────┐
│  Admin Comments                                          │
├─────────────────────────────────────────────────────────┤
│  [검색: ______] [Project: All ▼] [정렬: 최신순 ▼]       │
├─────────────────────────────────────────────────────────┤
│  Project         Author    Content         Date   Actions│
├─────────────────────────────────────────────────────────┤
│  Portfolio CMS   김철수    음저협 프로젝트... 2일 전 [🗑️]│
│  Portfolio CMS   Semin     감사합니다!...   2일 전 [🗑️]│
│  E-commerce      이영희    저도 비슷한...   3일 전 [🗑️]│
│  Mobile App      박민수    오프라인 동기화... 4일 전 [🗑️]│
└─────────────────────────────────────────────────────────┘
```

**2. 댓글 삭제**
- 확인 다이얼로그
- Cascade delete (대댓글도 함께 삭제)
- 프로젝트 stats.comments 자동 감소

**3. 필터링**
- 프로젝트별 필터
- 날짜 범위 필터
- 작성자 검색

### API Endpoints (MSW)

```typescript
// GET /api/admin/comments - 전체 댓글 목록 (모든 프로젝트)
// DELETE /api/admin/comments/:id - 댓글 삭제 (관리자 권한)
```

---

## 🎯 Phase 6.4: 권한 체크 및 보안

### AdminRoute 컴포넌트

```typescript
import { Navigate } from 'react-router-dom';
import { useAppSelector } from '../store/hooks';
import { selectUser } from '../store/slices/authSlice';

interface AdminRouteProps {
  children: React.ReactNode;
}

export const AdminRoute = ({ children }: AdminRouteProps) => {
  const user = useAppSelector(selectUser);

  // 로그인 안 됨 → 로그인 페이지로
  if (!user) {
    return <Navigate to="/login" replace />;
  }

  // Admin 아님 → 403 페이지로
  if (user.role !== 'admin') {
    return <Navigate to="/403" replace />;
  }

  // Admin 확인 → 렌더링
  return <>{children}</>;
};
```

### 라우터 적용

```typescript
// AppRouter.tsx
<Route
  path="/admin/projects"
  element={
    <AdminRoute>
      <AdminProjectsPage />
    </AdminRoute>
  }
/>
<Route
  path="/admin/comments"
  element={
    <AdminRoute>
      <AdminCommentsPage />
    </AdminRoute>
  }
/>
```

### useAdminCheck Hook

```typescript
export const useAdminCheck = () => {
  const user = useAppSelector(selectUser);

  const isAdmin = user?.role === 'admin';
  const isAuthenticated = !!user;

  return { isAdmin, isAuthenticated, user };
};
```

---

## 🎯 Phase 6.5: MSW Handlers (Admin API)

### 새로운 Admin Handlers

```typescript
// src/mocks/handlers/admin.ts

export const adminHandlers = [
  /**
   * GET /api/admin/projects - 관리자용 프로젝트 목록
   */
  http.get('/api/admin/projects', async ({ request }) => {
    // 권한 체크
    const authHeader = request.headers.get('authorization');
    if (!authHeader) {
      return HttpResponse.json({ success: false, error: 'Unauthorized' }, { status: 401 });
    }

    // 전체 프로젝트 반환 (public + draft)
    return HttpResponse.json({
      success: true,
      data: {
        items: mockProjects,
        total: mockProjects.length,
      },
    });
  }),

  /**
   * POST /api/admin/projects - 프로젝트 생성
   */
  http.post('/api/admin/projects', async ({ request }) => {
    const data = await request.json() as CreateProjectDto;

    const newProject: Project = {
      id: `${Date.now()}`,
      ...data,
      stats: { views: 0, likes: 0, comments: 0 },
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString(),
    };

    mockProjects.push(newProject);

    return HttpResponse.json({
      success: true,
      data: newProject,
    });
  }),

  /**
   * PUT /api/admin/projects/:id - 프로젝트 수정
   */
  http.put('/api/admin/projects/:id', async ({ params, request }) => {
    const { id } = params;
    const updates = await request.json() as UpdateProjectDto;

    const project = mockProjects.find((p) => p.id === id);
    if (!project) {
      return HttpResponse.json({ success: false, error: 'Not found' }, { status: 404 });
    }

    Object.assign(project, updates, {
      updatedAt: new Date().toISOString(),
    });

    return HttpResponse.json({
      success: true,
      data: project,
    });
  }),

  /**
   * DELETE /api/admin/projects/:id - 프로젝트 삭제
   */
  http.delete('/api/admin/projects/:id', async ({ params }) => {
    const { id } = params;

    const index = mockProjects.findIndex((p) => p.id === id);
    if (index === -1) {
      return HttpResponse.json({ success: false, error: 'Not found' }, { status: 404 });
    }

    // 프로젝트 삭제
    mockProjects.splice(index, 1);

    // 관련 댓글도 삭제
    mockComments = mockComments.filter((c) => c.projectId !== id);

    return HttpResponse.json({ success: true });
  }),

  /**
   * GET /api/admin/comments - 전체 댓글 목록
   */
  http.get('/api/admin/comments', async () => {
    return HttpResponse.json({
      success: true,
      data: {
        items: mockComments,
        total: mockComments.length,
      },
    });
  }),

  /**
   * DELETE /api/admin/comments/:id - 댓글 삭제
   */
  http.delete('/api/admin/comments/:id', async ({ params }) => {
    const { id } = params;

    // 댓글과 모든 대댓글 삭제
    const descendants = getDescendantIds(buildCommentTree(mockComments), id);
    mockComments = mockComments.filter((c) => c.id !== id && !descendants.includes(c.id));

    return HttpResponse.json({ success: true });
  }),

  /**
   * GET /api/admin/stats - 대시보드 통계
   */
  http.get('/api/admin/stats', async () => {
    const totalViews = mockProjects.reduce((sum, p) => sum + p.stats.views, 0);
    const totalLikes = mockProjects.reduce((sum, p) => sum + p.stats.likes, 0);

    return HttpResponse.json({
      success: true,
      data: {
        totalProjects: mockProjects.length,
        featuredProjects: mockProjects.filter((p) => p.featured).length,
        totalComments: mockComments.length,
        totalViews,
        totalLikes,
      },
    });
  }),
];
```

---

## 🎨 UI/UX 설계 원칙

### 1. 일관성
- MainLayout 사용 (Header + Sidebar)
- 색상 시스템 일관성 (CSS variables)
- 버튼/폼 스타일 통일

### 2. 직관성
- 명확한 액션 버튼 (+ 새 프로젝트, ✏️ 수정, 🗑️ 삭제)
- 아이콘 + 텍스트 조합
- 로딩 스피너 표시

### 3. 피드백
- 성공 메시지 (Toast)
- 에러 메시지 (Toast)
- 확인 다이얼로그 (삭제 전)

### 4. 반응형
- 모바일 대응 (테이블 → 카드 전환)
- 터치 친화적 버튼 크기

---

## 📝 구현 순서

### Step 1: 기초 설정 (30분)
1. ✅ AdminRoute 컴포넌트
2. ✅ useAdminCheck Hook
3. ✅ 403 Forbidden 페이지
4. ✅ 라우터 설정

### Step 2: Dashboard 메인 개선 (1시간)
1. ✅ StatsCard 컴포넌트
2. ✅ Admin Stats API (MSW)
3. ✅ DashboardPage 리팩토링
4. ✅ 최근 활동 목록

### Step 3: Admin Projects (2-3시간)
1. ✅ ProjectTable 컴포넌트
2. ✅ ProjectForm 컴포넌트 (모달)
3. ✅ AdminProjectsPage
4. ✅ Admin Projects API (MSW)
5. ✅ CRUD 기능 테스트

### Step 4: Admin Comments (1-2시간)
1. ✅ CommentTable 컴포넌트
2. ✅ AdminCommentsPage
3. ✅ Admin Comments API (MSW)
4. ✅ 삭제 기능 테스트

### Step 5: 최종 테스트 & Polish (1시간)
1. ✅ 모든 기능 end-to-end 테스트
2. ✅ 권한 체크 검증
3. ✅ UI/UX 개선
4. ✅ TypeScript 에러 체크

**총 예상 시간**: 5-7시간

---

## ✅ Success Criteria

### Functional Requirements
- [ ] Admin만 접근 가능 (non-admin은 403)
- [ ] 프로젝트 CRUD 완벽 작동
- [ ] 댓글 관리 완벽 작동
- [ ] 통계 실시간 업데이트
- [ ] Featured 설정 토글 작동

### Technical Requirements
- [ ] TypeScript 에러 0개
- [ ] No console errors
- [ ] MSW handlers 완벽 작동
- [ ] RTK Query cache 정상
- [ ] Optimistic updates 작동

### UI/UX Requirements
- [ ] 직관적인 인터페이스
- [ ] 반응형 디자인
- [ ] 로딩/에러 상태 표시
- [ ] 확인 다이얼로그 (위험 액션)
- [ ] Toast 알림 (성공/실패)

---

## 🚀 Production Checklist

### Security
- [x] Admin role 체크
- [x] 401/403 핸들링
- [ ] XSS 방지 (입력 sanitize)
- [ ] CSRF 토큰 (나중에 백엔드 연결 시)

### Performance
- [ ] 테이블 페이지네이션
- [ ] 이미지 lazy loading
- [ ] Debounced search
- [ ] Optimistic updates

### Accessibility
- [ ] Keyboard navigation
- [ ] ARIA labels
- [ ] Color contrast 4.5:1
- [ ] Screen reader 지원

### Testing
- [ ] Manual E2E 테스트
- [ ] 권한 체크 테스트
- [ ] CRUD 시나리오 테스트
- [ ] 에러 케이스 테스트

---

## 🎯 Next Steps After Phase 6

실제 백엔드 연결 시:
1. MSW → Real API 전환
2. MongoDB/Firebase 데이터베이스 연결
3. 이미지 업로드 (Cloudinary, S3)
4. SEO 최적화
5. Analytics 추가

**이제 구현을 시작하겠습니다!** 🚀
